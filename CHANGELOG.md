# 更新日志

本文件记录 `math-concept-coach` 的版本变化。写法遵循一个约定：**每条都讲"为什么改"，而不只是"改了什么"** —— 因为对技能而言，判断依据比条款本身更容易迁移。

版本号含义（非严格语义化，按"能力增量"递增）：

- **大版本（2.x）**：六棱镜骨架或输出产物形态发生结构性变化
- **小版本（1.x）**：新增一类能力、或修补一个成体系的薄弱面
- **补丁（1.x.y）**：措辞修正、路径修正、样例重生成

---

## [1.5] — 2026-09-13

### 为什么改

一次「拉真实运行产物对账」暴露了两个此前一直没看见的问题——都属于**绿灯下的静默失败**：

1. **sim 能力在中文 Windows 上跑不完整。** 脚本的标准输出一旦被**管道/重定向**捕获（Agent 正是这么读的），
   Python 就退回本地编码（简体中文为 GBK）；此时打印卡片里的 emoji（🎯）或质检自己的 `⚠ / ✓ / ✗`
   会抛 `UnicodeEncodeError`。后果是交付前最关键的两道闸门直接残废：`check_card.py` 的「举证清单」
   打到一半就断，`texmath.py` 自测当场崩溃——而这两条命令恰好写在 README 的「验证与测试」里。
2. **画廊生成器还是手工清单。** `make_gallery.py` 里硬编码着 3 张卡片，于是 v1.4 新加的《大数定律》卡
   根本没进画廊；这与它自己「增删卡片后重跑即可重建」的说明自相矛盾，统计栏还写死「交互组件 1 个」。

### 新增

- **` ```calc ` 概念计算器块**（第五种卡内块）：与 `sim` 共用同一套 Python / R 选项卡 UI，
  但语义是**确定性计算**——给「**一个函数 + 一次示例调用**」，输入参数就直接算出概念里的量
  （ε→N、ε→δ、n→上界、矩阵→秩、积分近似值……）。规范要求放在 **F5 之后单放一节**
  `## 🧮 概念计算器（输入 → 输出）`，每卡 ≤ 1 段。为「数学计算要有代码实现」提供了一等公民：
  滑块画不出、又不属于随机规律的**量化结论**，现在都有可复跑的代码兜底。
  样例：《数列极限》算 ε→N；《大数定律》算「样本量 ↔ 切比雪夫上界」。
- **`check_card.py` 的 calc 闸门**（`check_calc_blocks()`）：两段齐全、有「预期输出」、
  且要求**确定性**（用了随机数就警告、建议改 `sim`）；calc 块同样纳入「举证清单」与 `--run` 实跑。
- **画廊支持 sim / calc**：徽章区分「Python / R 模拟」「概念计算器」「Python / R 代码」，
  统计栏按卡片实际内容列出交互组件 / 模拟代码 / 计算器。

### 修复

- **非「面」小节不再被最后一面吞进折叠**：渐进揭示原先让每个面的正文一直延伸到**下一个面**，
  于是 F6 的折叠块吞掉了文末的自检 / 答案 / 核对提示（与函数 docstring 的承诺不符）。
  改为「每个面止于**下一个任意标题**」，这些小节（以及新的计算器节）默认可见。四张样例卡已按此重生成。
- **控制台输出统一 UTF-8**：在 `check_card.py`、`texmath.py`（原先崩溃）与 `md2card.py`、`make_gallery.py`
  （原先中文经管道捕获后乱码）的入口把 stdout/stderr 切到 UTF-8 并 `errors="replace"` 兜底。
  崩溃消除、中文不再乱码，**不改任何业务逻辑**。
- **画廊改为目录扫描**：`make_gallery.py` 扫描 `examples/*-概念卡.md`，`ORDER` 只定展示顺序；
  新增 `sim` 徽章（「Python / R 模拟」），并按卡片实际内容统计「交互组件 / 模拟代码 / 知识图谱 /
  GeoGebra」——补一张卡只需丢进目录、重跑脚本。
- **《大数定律》卡补 `认知动作：逼近（随机）`**（与《核心概念清单》一致，同时供画廊提取）。

### 修正

- `README.md` 文件结构里「make_demo_gif.py 已移出仓库」「smoke_test_widgets.mjs 未打进分发包」与事实不符
  （两者都在仓库内）——改为准确描述。
- `.claude-plugin/marketplace.json` 版本滞后（停在 1.3.0）——同步到 1.5.0。

### 验证

- 4 张样例卡跑 `check_card.py`：**Windows、经管道、不设任何编码环境变量**，全部 0 错。
- 《大数定律》跑 `check_card.py --run`：本机 Python 3.14 + R 4.6.1 **实跑 6/6 通过**
  （2 段 sim + 1 段 calc，各 Python / R 两语），预期数值全部命中——数学结果确实由代码跑出来。
  期间 `--run` 抓到一处真实笔误：Python 未格式化浮点时 `0.25/2.5` 会打印成 `0.09999999999999998`，
  与注释里的 `0.1` 不符——已改用 `:.4f` 并附实跑输出。这正是「预设输出必须实跑」的价值。
- 四张样例卡各配 1 段 `calc` 并 `--run` 实跑通过：一致连续（ε→δ，6/6）、数列极限（ε→N，3/3）、
  矩阵的秩（矩阵→秩）、大数定律（n↔切比雪夫上界，2/2）——Python / R 两语言输出一致。
- 无 pandoc 降级路径实跑：MathML + sim / calc 块 + 知识图谱均正常。
- `smoke_test_widgets.mjs` 5/5；画廊 4 张卡片的徽章、统计与真实产物一致。

---

## [1.4] — 2026-09-13

### 为什么改

v1.3 的演示手段全是**确定性**的：滑块拖动的几何关系。但有一类理解只能靠**跑起来**——
「频率为什么稳定到概率」「样本均值为什么越来越贴近期望」，这类**随机 / 迭代规律**画不出滑块图
（硬画就是一张假动画）。同时学生手里有 Python（必修课）和 R（统计课）两套环境，
只给一种语言就把另一半人挡在门外。

### 新增

- **` ```sim ` 模拟代码块**（第四种特殊块）：块内 `# PYTHON` / `# R` 两段划同一件事，
  卡片里渲染成**选项卡**——默认 Python，点标签切 R，右上角一键复制当前语言代码；
  **打印时两段全部展示**、各带语言标签（纸质版内容不缺失）。
  放置纪律：**「必须跑起来才能看见」才写**，散布在需要它的面（F1 验反例 / F3 验删条件 /
  F5 当随机规律的图形化替身 / 自检当「先猜输出再跑」的题），每卡 ≤ 2 个。
- **`scripts/check_card.py` 的 sim 闸门**（`check_sim_blocks()`）：语言段缺失 = 报错；
  随机数没固定种子、逐元素循环抽样、import 绘图库、缺「预期输出」注释 = 警告；
  每个 sim 块自动列入**举证清单**（预期输出必须实跑验证）。
- **样例卡《大数定律》**（概率论）：首张 sim 驱动的卡——频率稳定实验（Python/R 各一段）+
  样本均值收敛实验，两段预期输出均为本机 Python 3.14 / R 4.6.1 **实跑结果**。

### 约定

- **向量化纪律**：抽样与统计计算不写逐元素循环——Python 用 numpy（`np.random.default_rng(seed)`），
  R 用 tidyverse（`set.seed` + `tibble` + `map_dbl(\\(k) ...)`）或 `sapply`；for 只用于展示多档参数
  或真正的迭代递推。风格基准：张敬信《【Tidyverse优雅编程】概率论：两行代码模拟大数定律》。
- 对话内**不出现 sim 块**（选项卡是卡片专属形态）；对话里直接给两个普通代码块。
- F5 规则补充：概念本质是随机 / 迭代规律而滑块画不出来时，允许用 sim 替代 interactive 并写明理由——
  **不硬凑滑块图**（与既有「不硬编直观」红线同一精神）。

### 说明

- 规范：`references/widget-spec.md` 新增第九节（sim 写法与规则），原「写组件的纪律」顺延为第十节。
- `assets/card-widgets.js` 新增选项卡切换与复制逻辑（复制函数与 GeoGebra 块共用）；
  `assets/concept-card.css` 新增 sim 样式（屏幕 / 打印两态）；均通过 `smoke_test_widgets.mjs` 回归。

---

## [1.3] — 2026-09-11

### 为什么改

v1.2 的能力集中在"**讲清楚一个概念**"（六面拆解 + 三张样例卡）。回看下来有两个薄弱面：

1. **"懂了但不会做"没有出口。** 用户最常见的追问不是"这个概念是什么"，而是"概念我懂了，可题还是不会做""我怎么知道自己学会了"。v1.2 对此只能泛泛安慰，没有可执行动作。
2. **"讲对了"没有保障。** 不同教材对同一概念的定义入口、记号约定、分类口径并不完全一致（例如极限定义里 `n > N` 还是 `n ≥ N`、矩阵的秩从行秩还是列秩切入）。v1.2 无从表达"这是教材口径，请以你的教材为准"。

同时发现它天生适合跨客户端分发（纯 Markdown + 零依赖脚本），但文档层写了不少只在某一个客户端成立的表述。

### 新增

- **四套可执行程序**（`references/process-playbook.md`）：
  - 「两头往中间」证明书写程序 —— 从结论倒推、从条件正推，在中间对接；
  - 「条件反射清单」—— 把定义的条件逐条做成检查项，对付"看着会、下笔错"；
  - 「题量与难度判据」—— 什么时候停、什么时候加练，给出可判断的信号；
  - 「学完自测」—— 不依赖做题的自我验收动作。
- **反例构造六法**（`references/process-playbook.md`）与**误区来源六类**：把"举反例"从灵光一现变成可操作流程。
- **来源可信机制**（`references/source-integrity.md`，新文件）：三类标记 + 文末两段式处理
  - 〔已核〕有权威来源可直接确认；〔教材口径〕随教材而异，按主流口径写并注明；〔待核〕存在异议或无法确认。
  - 正文按教材口径给结论，文末统一给「⚠️ 核对提示（请以你所用教材为准）」。
- **机械闸门**（`scripts/check_card.py` 的 `check_source_marks()`）：把上面的约定从"纪律"变成"程序"——有〔待核〕却无核对提示块直接报错；〔教材口径〕无提示块给警告；有提示块却无标记也警告；标记合计超过 6 处警告（防止过度标注）。

### 改进

- **跨客户端通用性加固**：新增「运行环境」节；文档中 `python` → `python3`；把客户端专有表述（`present_files`、"用 Read 读取"）改为通用说法；`texmath.py` / `make_gallery.py` 补 shebang。
- **`.gitattributes`**（`* text=auto eol=lf`）：否则 Windows 检出为 CRLF 后，带 shebang 的脚本在 macOS / Linux 上会 `bad interpreter`。这是跨平台分发最容易踩的坑。
- **F2 ↔ F3 接缝**说明：通俗类比（F2）与严格表述（F3）之间怎么过渡，是六棱镜最容易断的一环。

### 修复

- 三张样例卡按 v1.3 规范**全量重生成**，新栏目（条件反射清单 / 学完自测 / checkbox）实测入 HTML。
- `examples/数列极限-概念卡.html` 曾被 WorkBuddy 编辑器注入 1595 处 `data-page-node-id`（164 KB）。**未直接覆盖**：先生成新文件证明"剥离该属性后两者仅差 `<hr/>` 与 `<hr />`（pandoc 版本差异）"，确认可无损重生成后再干净重生（96 KB）。

### 验证

- 打磨报告：`math-concept-coach-打磨报告.md`（含 14 节），出生证 **14 PASS / 0 WARN / 0 FAIL**。
- 存疑闸门**故障注入 4/4 命中**；三张样例卡 **0 错 0 警**。
- 通用性实证：`npx skills add zhjx19/math-concept-coach` 列出 **79 个 agent**（含 Claude Code / Codex / OpenCode / OpenClaw 等），安装副本实跑生成概念卡成功（138 公式 / 1 交互组件 / 1 知识图谱）。

---

## [1.2] — 2026-09-11

### 为什么改

v1.1 的顺序是"直接给六面拆解"，实测容易变成教师单向输出——学生看完点头，但缺一个"自己先想一下"的钩子，理解是接住的、不是长出来的。

### 改进

- **工作流改为"先引导、后拆解"**：定位概念 → 抛引导问题让学生先动手 → 再给六棱镜拆解 → 生成概念卡。
- **举证清单闸门**：输出前先对六面逐项自检，缺面不许收尾（防止"讲了一面就收"）。

### 新增

- **概念卡三类特殊块**可直接在卡片里渲染：
  - ` ```interactive ` 自绘交互组件（`assets/card-widgets.js`，5 类：plot / seq / riemann / matrix2 / vectors2，零依赖 Canvas）；
  - ` ```concept-map ` 知识图谱内联 SVG（`scripts/knowledge_map.py`，左前置 / 中概念 / 右后续 / 下对偶）；
  - ` ```geogebra ` 带"复制指令"按钮的 GeoGebra 命令块。
- **`scripts/smoke_test_widgets.mjs`**：交互组件回归测试，`node` 直接跑，不需要浏览器。

### 说明

v1.2 被反复用作出问题时的对照基线，已封存于 `math-concept-coach.baseline-v1.2-20260911`（24 文件）。

---

## [1.0] / [1.1] — 2026-09-10

创建当日即迭代，未单独留存正式记录。要点：

- 确立核心骨架 **「六棱镜」**：F1 正反拆解（含删条件实验）/ F2 通俗理解（类比必标失效边界）/ F3 严格数学表述 / F4 几何物理意义 / F5 图形化演示 / F6 联系其它概念。
- 确立设计理念：**做题是手段，掌握知识才是目的**；不教套路，只帮学生把定义读懂、把概念挂进知识体系。
- 产出可离线阅读的概念卡 HTML（pandoc `--mathml`，不依赖 MathJax CDN）。
- 产出首批样例概念卡（一致连续 / 数列极限 / 矩阵的秩）。

---

## 相关文件

- `SKILL.md` —— 技能主文档（工作流、纪律红线、自检清单）
- `README.md` —— 面向使用者的说明与安装方式
- `references/` —— 六棱镜逐面指南、过程程序、严格表述、来源可信、可视化与组件规范
- `examples/` —— 三张样例概念卡（`.md` 源 + `.html` 产物）与核心概念清单
